[package]
name = "neural-space"
version = "0.1.0"
edition = "2024"
description = "Deterministic autonomic nervous system for hypersonic aerospace vehicles"

[dependencies]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
